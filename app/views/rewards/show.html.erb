<h1>Rewards#show</h1>

ログインユーザ：<%= current_user.name %>
<br />ご褒美<br />
<%= @reward.completiondate %>に　
<%= @reward.location %>で　
<%= @reward.content %>する
<%= link_to "ご褒美の編集", edit_reward_path(@reward) %>

<br />目標<br />
<% @reward.goals.each do |goal| %>
  ユーザ名：<%= goal.user.name %>
  目標：<%= goal.content %>
  進捗率：<%= goal.progress %>%
  <%= link_to "目標の編集", edit_goal_path(goal) %>
<% end %>
<%= button_to "削除", @reward, method: :delete %>

<br />
<% @reward.users.each do |user| %>
  関連ユーザ名：<%= user.name %>
  関連ユーザメール：<%= user.email %>
  <br />
<% end %>
